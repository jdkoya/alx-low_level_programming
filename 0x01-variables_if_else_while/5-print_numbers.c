#include <stdio.h>
/**
 * main - A programme programme that prints all single digit numbers of base 10
 * Return: 0 (seccessful)
 */
int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
	{
		printf("%d", n);
	}
	putchar('\n');
	return (0);
}
