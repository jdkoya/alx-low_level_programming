#include <stdio.h>
/**
 * main - A programme that prints in reverse
 * Return: 0 (seccessful)
 */
int main(void)
{
	char k;

	for (k = 0; k < 10; k++)
	{
		putchar(k);
	}
	putchar('\n');
	return (0);
}
