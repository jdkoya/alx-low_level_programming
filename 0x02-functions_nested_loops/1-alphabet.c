#include "main.h"

/** programme that prints the size of variables types
  * Return: 0 (seccessful)
  */

void print_alphabet(void);
{
	char k;

	for(k = 'a'; k <= 'z'; k++)
		_putchar(k);
	_putchar('\n');
}
